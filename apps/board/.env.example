# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co

# Publishable Key (starts with "sb_publishable_" or "anon_")
# - Safe for client-side use (public, low privilege)
# - Protected by Row Level Security (RLS) policies
# - Easy zero-downtime rotation
# - Get from: Project Settings > API > Project API keys > "anon" "public" key
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key_here

# IMPORTANT: NEVER use service_role or secret keys in frontend!
# ❌ NEVER: VITE_SUPABASE_SERVICE_ROLE_KEY=...
# ❌ NEVER: VITE_SUPABASE_SECRET_KEY=sb_secret_...
# ❌ NEVER: Legacy JWT keys (eyJ...) - deprecated and no longer supported

# Migration from Legacy Keys:
# If you're upgrading from the old JWT-based anon key (eyJ...):
# 1. Go to your Supabase dashboard: Project Settings > API
# 2. Look for the "anon" "public" key (not the service_role key!)
# 3. Copy that key and use it as VITE_SUPABASE_PUBLISHABLE_KEY
# 4. The anon/public key is the safe, client-side key (same security level as old JWT)
# 5. Update your .env file and restart your dev server

# Application Configuration
VITE_APP_NAME=SolidJS Task Board
VITE_APP_ENV=development
VITE_APP_URL=http://localhost:3000

# Optional: Feature Flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=true
VITE_ENABLE_REALTIME=true